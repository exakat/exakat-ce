name = "Static Methods Cannot Call Non-Static Methods";
description = "A static method cannot call a non-static method. The object context would be missing. 

On the other hand, a method may call a static method, as the context is lost, but not useful. 

Magic methods cannot be static, so they are out of this rule. This applies to the constructor, when called with ``parent::__construct()``.
";
code = "<?php

class x {
	function foo() {}

	static function ioo() {
		// This syntax is valid within a class
		// yet, the call is not possible
		self::foo();
	}

}
?>";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.6.3";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
precision = "P_VERY_HIGH";
;Versions of PHP where this rule is applicable. 
;8.0+, 7.4-...
phpVersion = ""

; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
;phpDeprecation = "7.4";

; List of links of interest
seeAlso[] = "";

; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features
features[] = "";

; Possible other rules that might be related to this one. 
; Use 'Folder/Name' format, one each
; related[] = ""

; Alternative to make this code go away. 
; One by possible solution
modifications[] = "Make the calling method non static too";
modifications[] = "Remove the call to the non-static method";
modifications[] = "Make the target method static";

; A PHP error that may be emitted by the target faulty code
phpErrors[] = "Non-static method x::foo() cannot be called statically"

; Human readable keywords for manual glossary
keywords[] = "lazy loading"

; manual index for Sphinx index building
;index[] = ""

; Optional parameters
; Duplicate and increment numbers to add more : parameter1, paramter2, etc. 
;[parameter1]
;name="parametersCount";
;default="8";
;type="integer";
;description="Minimal number of parameters to report.";
