name = "Magic Visibility";
description = "Magic methods must be declared with public visibility. They cannot be private or protected.

Magic methods cannot be declared as static. They are always associated with an instance of a class and cannot be called statically.

<?php

class foo{
    // magic method must bt public and non-static
    public static function __clone($name) {    }

    // magic method can't be private
    private function __get($name) {    }

    // magic method can't be protected
    private function __set($name, $value) {    }

    // magic method can't be static
    public static function __isset($name) {    }
}

?>

";
clearphp = "";
phpVersion = "5.4-";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "0.8.4";
precision = "P_VERY_HIGH"

modifications[] = ""

features[] = "visibility"
features[] = "magic-method"

seeAlso[] = "`Magic methods <https://www.php.net/manual/en/language.oop5.magic.php>`_"
seeAlso[] = "`PHP Magic Methods Explained <https://atakde.medium.com/php-magic-methods-explained-bac7053c007d>`_"

phpErrors[] = "The magic method x::__call() must have public visibility"
