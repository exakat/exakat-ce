name = "Integer As Property";
description = "It is backward incompatible to use integers are property names. This feature was introduced in PHP 7.2.

If the code must be compatible with previous versions, avoid casting arrays to object.

<?php

// array to object
$arr = [0 => 1];
$obj = (object) $arr;
var_dump(
    $obj,
    $obj->{'0'}, // PHP 7.2+ accessible
    $obj->{0} // PHP 7.2+ accessible

    $obj->{'b'}, // always been accessible
);
?>

";
clearphp = "";
phpVersion = "7.2+";
severity = "S_MAJOR";
timetofix = "T_SLOW";
exakatSince = "1.0.4";
precision = "P_HIGH"
seeAlso[] = "`PHP RFC: Convert numeric keys in object/array casts <https://wiki.php.net/rfc/convert_numeric_keys_in_object_array_casts>`_"
features[] = "property"
phpErrors[] = ""
modifications[] = "add a prefix with letters whenever property's name adaptation is possible"