name = "Constant Class";
description = "A class or an interface only made up of constants. Constants usually have to be used in conjunction of some behavior (methods, class...) and never alone. 

<?php

class ConstantClass {
    const KBIT = 1000;
    const MBIT = self::KBIT * 1000;
    const GBIT = self::MBIT * 1000;
    const PBIT = self::GBIT * 1000;
}

?>

As such, they should be PHP constants (build with define or const), or included in a class with other methods and properties. 

";
clearphp = "";
severity = "S_MINOR";
timetofix = "T_SLOW";
exakatSince = "0.8.4";
precision = "P_HIGH"

rulesetBuild_title="Constant Class"
rulesetBuild_code = "class x {
    const X1 = 1; 
    // may be many constants
    // no properties, no methods
}"

modifications[] = "Make the class an interface"
modifications[] = "Make the class an abstract class, to avoid its instantiation"

features[] = 'class-constant'

seeAlso[] = " `PHP Classes containing only constants <https://stackoverflow.com/questions/16838266/php-classes-containing-only-constants>`_"

phpErrors[] = ""

phpVersion = "";
