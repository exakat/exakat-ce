name = "Too Many Finds";
description = "Too many methods called 'find*' in this class. It is may be time to consider the `Specification pattern <https://en.wikipedia.org/wiki/Specification_pattern>`_.

<?php

// quite a fishy interface
interface UserInterface {
    public function findByEmail($email);
    public function findByUsername($username);
    public function findByFirstName($firstname);
    public function findByLastName($lastname);
    public function findByName($name);
    public function findById($id);

    public function insert($user);
    public function update($user);
}

?>

";
clearphp = "";
severity = "S_MINOR";
timetofix = "T_SLOW";
exakatSince = "0.10.5";
precision = 'P_VERY_HIGH'

modifications[] = 'Split the class into smaller classes'
modifications[] = 'Remove some of the find* methods'

features[] = 'class'

seeAlso[] = "`On Taming Repository Classes in Doctrine <https://beberlei.de/2013/03/04/doctrine_repositories.html>`_"
seeAlso[] = "`specifications <https://slides.pixelart.at/2017-02-04/fosdem/specifications/#/>`_"

phpErrors[] = ""

phpVersion = "";



[parameter1]
name="minimumFinds";
default="5";
type="integer";
description="Minimal number of prefixed methods to report.";

[parameter2]
name="findPrefix";
default="find";
type="string";
description="list of prefix to use when detecting the 'find'. Comma-separated list, case insensitive. ";

[parameter3]
name="findSuffix";
default="";
type="string";
description="list of fix to use when detecting the 'find'. Comma-separated list, case insensitive. ";



