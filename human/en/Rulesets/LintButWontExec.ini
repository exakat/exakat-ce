name="LintButWontExec"
short="Check the code for common errors that will lead to a Fatal error on production, but lint fine."
description = "This ruleset focuses on PHP code that lint (php -l), but that will not run. As such, this ruleset tries to go further than PHP, by connecting files, just like during execution."
reports[] = ""