name = "glob";
id = "glob"
alias[] = ""
description = "glob() is a function that finds pathnames matching a pattern.

The glob() function is not related to the global keyword.
"
code = "<?php
foreach (glob(""*.txt"") as $filename) {
    echo ""$filename size "" . filesize($filename) . ""\n"";
}
?>"
documentation = "https://www.php.net/manual/en/function.glob.php"
analyzers[] = "Performances/NoGlob"
analyzers[] = "Structures/NoHardcodedPath"
cobblers[] = ""
;phpVersionSince = "4.0"
;phpVersionUntil = "9.0"

related[] = ""

rule = ""

popularity = ""