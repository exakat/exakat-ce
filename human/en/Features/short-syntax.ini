name = "Short Syntax";
id = "short-syntax"
alias[] = ""
description = "A short syntax is the fusion of an operator with the `=` operator. That way, the operator is applied to the right hand operand, and the left variable, then, assigned to that variable. 

It is convenient when the variable has to be assigned again to itself.
"
code = "<?php

$a = 0;
// Adds 2 to $a
$a = $a + 2;

// short syntax to add 3 to $a
$a += 3;

?>"
documentation = "https://www.php.net/manual/en/language.operators.assignment.php"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
related[] = 'assignation'
related[] = 'operator'
