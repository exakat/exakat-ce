name = "Regular Expressions";
id = "regex"
alias[] = ""
description = "A regular expression is a sequence of characters that specifies a search pattern. PHP currently uses the PCRE library, and used to have 2 such libraries. "
code = "<?php
  preg_match('/(foo)(bar)(baz)/', 'foobarbaz', $matches, PREG_OFFSET_CAPTURE);
  print_r($matches);
?>"
documentation = "https://www.php.net/preg_match"
analyzers[] = "Php/MissingSubpattern"
analyzers[] = "Php/PregMatchAllFlag"
cobblers[] = ""
phpVersionSince = "4.0"
;phpVersionUntil = "9.0"
related[] = ''
