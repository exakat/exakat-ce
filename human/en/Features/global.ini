name = "global Scope";
id = "global"
alias[] = ""
description = "The global scope is the top level scope. It is the scope when no function, or method scope is active. It is also the first scope when starting execution. 

Variables from the global scope are accessible in other scopes by using the `global` keyword, or the $GLOBALS variable. Variables in the global scope are automatically added to the $GLOBALS variable; in local scopes, the `global` keyword have that effect too.
"
code = "<?php

$x = 'A';

function counter() {
    echo $GLOBALS['A'];

	global $x;
	
	echo $x;
}

foo();

?>"
documentation = "https://www.php.net/manual/en/reserved.variables.globals.php"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
related[] = "variable"
related[] = "static-variable"
related[] = "variable-variable"
related[] = "global-variable"
tags[] = "variable"
tags[] = "feature"
seeAlso[] = ""
