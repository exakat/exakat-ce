name = "__sleep() method";
id = "__sleep"
alias[] = ""
description = "__sleep() is a magic method : it is called an object is being serialized, so that the object may be cleaned. 

In particular, some resources, such as database connexions or files cannot be serialized. They should be closed, stored as a path, and reopened later with __wakeup().

The __sleep() method is usually paired with the __wakeup() method. 

"
code = "<?php

class x {
	private $file;
	private $path;

	function __sleep() {
		fclose($this->path);
	}

	function __wakeup() {
		$this->path = fopen($this->path, 'r');
	}
}

?>"
documentation = "https://www.php.net/manual/en/language.oop5.magic.php#object.sleep"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
related[] = "magic-method"
related[] = "__wakeup"
seeAlso[]=  "`PHP __sleep And __wakeup Magic Methods: How And When To Use Them? <https://medium.com/@lukaspereyra8/php-sleep-and-wakeup-magic-methods-how-and-when-to-use-them-938591584bdc>`_"
seeAlso[]=  "`__sleep() and __wakeup() <https://riptutorial.com/php/example/4604/--sleep---and---wakeup-->`_"
tags[] = "magic method"