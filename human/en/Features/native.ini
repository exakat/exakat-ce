name = "Native"
id = "native"
alias[] = ""
description = "A features is called 'native' when it is part of the standard installation of PHP. For example, `.` (dot operator) or the `stdClass` class are native to PHP.
"
code = "<?php

// This is a native function
$s = strtolower($s);

// This is not a native function : it comes from an extension
$rar_arch = RarArchive::open('latest_winrar.rar');


?>"
documentation = ""
analyzers[] = ""
;cobblers[] = "Classes/VarToPublic"
;phpVersionSince = ""
;phpVersionUntil = "9.0"
