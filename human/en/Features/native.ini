name = "Native"
id = "native"
alias[] = "native-function"
alias[] = "native-class"
alias[] = "native-constant"
description = "A feature is called 'native' when it is part of the standard installation of PHP. For example, `.` (dot operator) or the `stdClass` class are native to PHP.

A feature may also be custom, when defined by the developer or extension, when defined in a PHP extension. 
"
code = "<?php

// This is a native function
$s = strtolower($s);

// This is not a native function : it comes from an extension
$rar_arch = RarArchive::open('latest_winrar.rar');


?>"
documentation = ""
analyzers[] = ""
cobblers[] = "Classes/VarToPublic"
phpVersionSince = ""
phpVersionUntil = ""
related[] = ""
seeAlso[] = ""
