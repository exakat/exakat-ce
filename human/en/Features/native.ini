name = "Native"
id = "native"
alias[] = "native-function"
alias[] = "native-class"
alias[] = "native-constant"
alias[] = "built-in"
description = "A feature is called 'native' or 'built-in' when it is part of the standard installation of PHP. For example, `.` (dot operator) or the `stdClass` class are native to PHP.

On the other hand, a feature is custom : that is, when defined by the developer or a PHP extension. "
code = "<?php

// This is a native function
$s = strtolower($s);

// This is not a native function : it comes from an extension
$rar_arch = RarArchive::open('latest_winrar.rar');


?>"
documentation = ""
analyzers[] = ""
cobblers[] = "Classes/VarToPublic"
phpVersionSince = ""
phpVersionUntil = ""
related[] = "built-in"
seeAlso[] = ""
phpVersionDeprecated = "";
popularity = "";
rule = "";
packagist[] = "";
tags[] = "";
