name = "Array spread";
id = "array-spread"
alias[] = ""
description = "Array spread is the ellipsis operator, applied to an array. Then, the array itself is replaced by each of its own element, as if each element was spread from the array.
"
code = "<?php

$a = [1,2,3];
$b = [...$a, 4,5];

// $b === [1,2,3,4,5];

?>"
documentation = "https://www.php.net/manual/en/migration74.new-features.php#migration74.new-features.standard.array-merge-no-args"
analyzers[] = "Php/SpreadOperatorForArray"
analyzers[] = "Arrays/NoSpreadForHash"
cobblers[] = ""
;phpVersionSince = "4.0"
;phpVersionUntil = "9.0"

related[] = "ellipsis"

rule = ""

popularity = ""