name = "Short Ternary Operator";
id = "short-ternary"
alias[] = "short-ternary-operator"
description = "The expression ``(expr1) ?: (expr3)`` evaluates to ``expr2`` if ``expr1`` evaluates to empty(), and ``expr3`` if ``expr1`` evaluates to false.

Is is a short version of the ternary operator, and it is often used to set default values in a concise way.
"
code = "<?php

$action = (empty($_POST['action'])) ? 'default';

?>"
documentation = "https://www.php.net/manual/en/language.operators.comparison.php#language.operators.comparison.ternary"
analyzers[] = "Php/ShortTernary"
cobblers[] = ""
phpVersionSince = "5.3+"
phpVersionUntil = ""
seeAlso[] = "`Short Hand Comparison in PHP <https://stitcher.io/blog/shorthand-comparisons-in-php>`_";
related[] = "ternary"
related[] = "coalesce"
tags[] = "feature"
tags[] = "operator"
