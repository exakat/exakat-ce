name = "Iterator";
id = "iterator"
alias[] = ""
description = "An iterator is an object which can be traversed with a foreach() loop. 

`Iterator` is an interface, which shall be implemented by objects. Ther are also a list of default iterators in the SPL extension.

An iterator may be turned into an array, with the iterator_to_array() function.
";
code = "
<?php

class myIterator implements Iterator { /**/ }

$it = new myIterator;

foreach($it as $key => $value) {
    var_dump($key, $value);
    echo "\n";
}

?>
"
documentation = "https://www.php.net/manual/en/class.iterator.php"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
related[] = "spl"
related[] = "traversal"
related[] = "foreach"
seeAlso[] = "`Modern PHP Developer - Iterator <https://startutorial.com/view/modern-php-developer-iterator>`_"
seeAlso[] = "`Iterator in PHP <https://refactoring.guru/design-patterns/iterator/php/example>`_"
tags[] = "iterator"
tags[] = "interface"
