name = "Tick";
id = "tick"
alias[] = ""
description = "ticks are events that occur for a group of tickable statements executed by the parser within the declare block. 


"
code = "<?php

declare(ticks=1);

// A function called on each tick event
function tick_handler()
{
    echo 'tick_handler() called'.PHP_EOL;
}

register_tick_function('tick_handler'); // causes a tick event

$a = 1; // causes a tick event

if ($a > 0) {
    $a += 2; // causes a tick event
    print($a); // causes a tick event
}

?>"
documentation = "https://www.php.net/manual/en/control-structures.declare.php#control-structures.declare.ticks"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
seeAlso[] = ""
related[] = 'declare'
