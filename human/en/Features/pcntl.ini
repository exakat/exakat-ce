name = "Process Control (pcntl)";
id = "pcntl"
alias[] = ""
description = "Process Control is a set of native PHP function, that handles signals coming from the OS. 

pcntl is suited for CLI operations, and not for web serving operations. 

pcntl manage process creation, program execution, signal handling and process termination. 

"
code = "<?php

$pid = pcntl_fork();
if ($pid == -1) {
     die('could not fork');
} else if ($pid) {
     // we are the parent
     pcntl_wait($status); //Protect against Zombie children
} else {
     // we are the child
}

?>
"
documentation = "https://www.php.net/manual/en/book.pcntl.php"
analyzers[] = ""
cobblers[] = ""
phpVersionSince = ""
phpVersionUntil = ""
seeAlso[] = "` Example: Parallel processing in PHP using pcntl_fork()  <https://gist.github.com/nicksantamaria/21dce5ff2a6640cdff76ce7bc57d2981>`_"
related[] = "handler"
related[] = ""
tags[] = ""