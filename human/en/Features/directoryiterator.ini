name = "DirectoryIterator";
id = "directoryiterator"
alias[] = ""
description = " The DirectoryIterator class provides a simple interface for viewing the contents of filesystem directories. "
code = "<?php
$iterator = new DirectoryIterator(__DIR__);
while($iterator->valid()) {
    $file = $iterator->current();
    echo $iterator->key() . "" => "" . $file->getFilename() . ""\n"";
    $iterator->next();
}
?>"
documentation = "https://www.php.net/manual/en/class.directoryiterator.php"
analyzers[] = ""
cobblers[] = ""
;phpVersionSince = "4.0"
;phpVersionUntil = "9.0"

related[] = "spl"

rule = ""

popularity = ""