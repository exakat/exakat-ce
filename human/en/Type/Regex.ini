name = "Regex Inventory";
description = "All regex used in the code. PHP has the PCRE extension that handles all regex : preg_match(), preg_replace(), etc. 

<?php

// PCRE regex used with preg_match
preg_match('/[abc]+/', $string);

// Mbstring regex, in the arabic range
if(mb_ereg('[\x{0600}-\x{06FF}]', $text))

?>

mbstring regexes are also collected. Pre-PHP 7.0 POSIX regex are not listed. 

See also  and 
         .
";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "0.12.14";
precision = "P_HIGH";
seeAlso[] = "`preg_match() <https://www.php.net/preg_match>`_";
seeAlso[] = "`ext/mbstring <http://www.php.net/manual/en/book.mbstring.php> `_";
seeAlso[] = "`ext/pcre <http://www.php.net/manual/en/book.pcre.php> `_";
features[] = "regex"
modifications[] = "";
phpErrors[] = "Delimiter must not be alphanumeric or backslash";
phpErrors[] = "No ending delimiter '/'";
