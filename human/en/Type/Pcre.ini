name = "Perl Regex";
description = "List of all the Perl Regex (PCRE-style).

<?php

preg_match('/[abc]/', $haystack);

preg_replace('#[0-9A-Z]+#is', $y, $z);

?>

Regex are spotted when they are literals : dynamically built regex, (including /$x/) are not reported.";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "0.8.4";
precision = "P_HIGH"

seeAlso[] = "`PCRE <https://www.php.net/manual/en/book.pcre.php>_";

features[] = "regex"

phpErrors[] = ""

phpVersion = ""

modifications[] = ""