name = "Add Brackets To Single Instructions";
description = "This cobbler adds curly brackets to single expression, with for(), foreach(), while(); and do...while() instructions. 

No brackets are added to instructions that are already bracketed.";

dependsOn[] = "";
exakatSince = "2.4.6";

beforeCode = "<?php

if ($a) 
	$b = 1;
else {
	$c = ;2
}

?>";
afterCode = "<?php

if ($a) {
	$b = 1;
} else {
	$c = ;2
}

?>";

;related PHP features
features[] = "block"

;List of possible opposite cobbler (there might be several)
reverseCobbler[] = "Structures/RemoveBracketsAroundSingleInstruction.ini"

;List of possible related analysis
suggestedAnalysis[] = ""

;similar cobbler; alternative cobbler.
relatedCobbler[] = ""

;Is the cobbler idempotent (run once only)
idempotent=1

;PHP versions where this is applicable
phpVersion = ""

; keywords for documentation
keywords[] = "coding-convention"
