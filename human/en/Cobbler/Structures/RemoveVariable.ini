name = "Remove Written Only Variable";
description = "This removes variables that are written only. ";

dependsOn[] = "";
exakatSince = "2.3.0";

beforeCode = "<?php

function foo() {
    $a = 1;
    $a += 2; // No usage of $a
}

?>";
afterCode = "<?php

function foo() {
}

?>";

;related PHP features
features[] = "variable"

;List of possible opposite cobbler (there might be several)
reverseCobbler[] = ""

;List of possible related analysis
suggestedAnalysis[] = 'Variables/WrittenOnlyVariable';

;similar cobbler; alternative cobbler.
relatedCobbler[] = ""

;Is the cobbler idempotent (run once only)
idempotent=1

phpVersion = ""

keywords[] = ""