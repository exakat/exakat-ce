name = "Logical To in_array()";
description = "This cobbler turns lists of ``or`` calls into a in_array() call. This is a faster and more readable expression.";

dependsOn[] = "";
exakatSince = "2.6.1";

beforeCode = "<?php

if ($a == 1 || $a == 2) {
	// doSomething()
}

?>";
afterCode = "<?php

if (in_array($a, [1, 2])) {
	// doSomething()
}

?>";

;related PHP features
features[] = ""

;List of possible opposite cobbler (there might be several)
reverseCobbler[] = ""

;List of possible related analysis
suggestedAnalysis[] = "Performances/LogicalToInArray"

;similar cobbler; alternative cobbler.
relatedCobbler[] = ""

;Is the cobbler idempotent (run once only)
idempotent=1

;PHP versions where this is applicable
phpVersion = ""

; keywords for documentation
keywords[] = "performance"

;[parameter1]
;name="parameterName";
;default="parameterValue";
;type="data";
;description="What does the parameter control";
