name = "Remove Dollar Curly";
description = "This cobbler transforms the ``${ }`` structure into ``{$ }``. It is assumed that the content of the curly braces are only a variable name.

This update is important for PHP 8.2, where the syntax is deprecated.

";

dependsOn[] = "";
exakatSince = "2.3.0";

beforeCode = "<?php

$a = \"${b}\";

?>";
afterCode = "<?php

$a = \"{$b}\";

?>";

;related PHP features
features[] = "string-interpolation"

;List of possible opposite cobbler (there might be several)
reverseCobbler[] = ""

;List of possible related analysis
suggestedAnalysis[] = ""

;similar cobbler; alternative cobbler.
relatedCobbler[] = ""

;Is the cobbler idempotent (run once only)
idempotent=1

phpVersion = ""

keywords[] = ""

;[parameter1]
;name="parameterName";
;default="parameterValue";
;type="data";
;description="What does the parameter control";
