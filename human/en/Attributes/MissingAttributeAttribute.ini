name = "Missing Attribute Attribute";
description = "While not strictly required it is recommended to create an actual class for every attribute.

<?php

namespace Example;

use Attribute;

#[Attribute]
class MyAttribute
{
}

#Missing the above attribute
class MyOtherAttribute
{
}

?>

";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.2.4";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
precision = "P_VERY_HIGH";
;Versions of PHP where this rule is applicable. 
;8.0+, 7.4-...
phpVersion = "8.0+"

seeAlso[] = "`Declaring Attribute Classes <https://www.php.net/manual/en/language.attributes.classes.php>`_";

; Possible other rules that might be related to this one. 
; Use 'Folder/Name' format, one each
 related[] = ""
 
 features[] = "attribute"

; Alternative to make this code go away. 
; One by possible solution
modifications[] = "Add the Attribute attribute to those classes"

; A PHP error that may be emitted by the target faulty code
phpErrors[] = ""
