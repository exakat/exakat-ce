name = "Fallback Function";
description = "A function that is called with its name alone, and whose definition is in the global scope.

<?php

namespace {
    // global definition
    function foo() {}
}

namespace Bar {
    // local definition
    function foo2() {}
    
    foo(); // definition is in the global namespace
    foo2(); // definition is in the Bar namespace
}

?>

";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "1.1.4";
precision = 'P_VERY_HIGH'

modifications[] = ''

features[] = 'function'
features[] = 'fallback-function'

seeAlso[] = "`Using namespaces: fallback to global function/constant <https://www.php.net/manual/en/language.namespaces.fallback.php>`_"

phpErrors[] = ""

phpVersion = "";
