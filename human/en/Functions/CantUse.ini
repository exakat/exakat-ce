name = "Methods That Should Not Be Used";
description = "These methods and functions only throw an exception, or raise an error. As such, they are a warning that such function or method shouldn't be used. 

<?php

function obsoleteFoo() {
    throw new exception('Don\'t use obsoleteFoo() but rather the new version of foo().');
}
?>

Those functions could also be marked as deprecated, with an attribute or a phpdoc. This is not taken into account by this analysis.

";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "1.8.7";
precision = "P_VERY_HIGH"

modifications[] = ""

features[] = "function"
features[] = "deprecated"

seeAlso[] = ""

phpErrors[] = ""

phpVersion = "";
