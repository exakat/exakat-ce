name = "func_get_arg() Modified";
description = "func_get_arg() and func_get_args() used to report the calling value of the argument until PHP 7. 

Since PHP 7, it is reporting the value of the argument at calling time, which may have been modified by a previous instruction. 

This code will display 1 in PHP 7, and 0 in PHP 5.";
code = "<?php

function x($a) {
    print func_get_arg(0);  // 0 
    $a++;
    print func_get_arg(0);  // 1
}

x(0);
?>";
clearphp = "";
severity = "S_MAJOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "0.8.4";
precision = "P_HIGH";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
modifications[] = "Use func_get_arg() early in the function.";
modifications[] = "Avoir mixing func_get_args() and direct access to the parameters.";
modifications[] = "Avoir using func_get_args() and specifying parameters.";
modifications[] = "Avoir modifying parameters.";

; Alternative to make this code go away. 
; One by possible solution

features[] = "arbitrary-argument";
; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features

seeAlso[] = "";
; List of links of interest

related[] = "";
; Possible other rules that might be related to this one. 
; Use "Folder/Name" format, one each
phpVersion = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.

phpDeprecation = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
phpErrors[] = "";
; A PHP error that may be emitted by the target faulty code

keywords[] = "";
; Human readable keywords for manual glossary

changedBehavior = "7.0";
; Which version did that behavior changed
changedBehavior_details = "";

