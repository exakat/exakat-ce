name = "File Is Not Definitions Only";
description = "An included file should only provide definitions and declarations, or executable code : not both. 

With definitions only files, their inclusion provide new features, and keep the current execution untouched, and in control of the flow.

<?php
// This whole script is a file

// It contains definitions and global code
class foo {
    static public $foo = null;
}
//This can be a singleton creation
foo::$foo = new foo();

trait t {}

class bar {}

?>

Within this context, globals, use, and namespaces instructions are not considered a warning.";
clearphp = "";
severity = "S_MINOR";
timetofix = "T_SLOW";
exakatSince = "0.8.4";
precision = "P_VERY_HIGH"
seeAlso[] = ""

features[] = ""

phpErrors[] = ""

phpVersion = ""

modifications[] = "Remove the executable code from the file"
modifications[] = "Remove the definitions from the file"
