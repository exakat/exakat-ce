name = "GLOB_BRACE Usage";
description = "GLOB_BRACE is not always available on every underlying operating system. This is the case on Solaris OS, and on Alpine OS, used for Docker.

<?php

// glob uses GLOB_BRACE
$abcFiles = glob($path.'/{a,b,c}*', GLOB_BRACE); 

// avoiding usage of GLOB_BRACE
$abcFiles = array_merge(glob($path.'/a*'), 
                        glob($path.'/b*'), 
                        glob($path.'/c*'), 
                       ); 

?>

It is possible to check the support for GLOB_BRACE by checking the presence of the constant. 

";
clearphp = "";
severity = "S_MAJOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.1.6";
precision = "P_VERY_HIGH"

features[] = "glob"

seeAlso[] = "`Alpine Linux <https://alpinelinux.org/>`_"
seeAlso[] = "`GLOB_BRACE breaks Sulu on Alpine Linux <https://github.com/sulu/sulu/issues/4513>`_"
seeAlso[] = "`[performance] Symfony Kernel::boot() in dev mode on Alpine #35009 <https://github.com/symfony/symfony/issues/35009>`_"

; Alternative to make this code go away. 
; One by possible solution
modifications[] = "Create as many glob() calls at there are alternative in the braces"
modifications[] = "Use another tool to search the system on names"
modifications[] = "Do not use glob brace"

; A PHP error that may be emitted by the target faulty code
phpErrors[] = ""
phpVersion = ""
