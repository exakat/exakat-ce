name = "Null Or Boolean Arrays";
description = "Null, int, floats, booleans are valid with PHP array syntx. Yet, they only produces ``null`` values. They also did not emits any warning until PHP 7.4.

Older code used to initialize variables as null, sometimes explictly, and then, use them as arrays. The current support for this syntax is for backward compatibility. 

Illegal keys, such as another array, will also generate a NULL value, instead of a Fatal error. 
";
code = "<?php

// outputs NULL
var_dump(null[0]);
var_dump(null[[]]);

const MY_CONSTANT = true;
// outputs NULL
var_dump(MY_CONSTANT[10]);

?>";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "1.8.6";
precision = "P_VERY_HIGH";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
modifications[] = "Avoid using the array syntax on null and boolean";
modifications[] = "Avoid using null and boolean on constant that are expecting arrays";
; Alternative to make this code go away. 
; One by possible solution

features[] = "null";
features[] = "boolean";
features[] = "float";
features[] = "int";
features[] = "array";
; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features

seeAlso[] = "`Null and True <https://twitter.com/Chemaclass/status/1144588647464951808>`_";
; List of links of interest

related[] = "";
; Possible other rules that might be related to this one. 
; Use "Folder/Name" format, one each
phpVersion = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.

phpDeprecation = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
phpErrors[] = "Trying to access array offset on value of type null";
phpErrors[] = "Trying to access array offset on value of type boolean";
phpErrors[] = "Trying to access array offset on value of type float";
phpErrors[] = "Trying to access array offset on value of type int";
; A PHP error that may be emitted by the target faulty code

keywords[] = "backward compatibility";
; Human readable keywords for manual glossary
