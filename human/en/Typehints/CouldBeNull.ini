name = "Could Be Null";
description = "Mark arguments, properties, class constants and return types that can be ``null``. Null was introduced as a standlone type in PHP 8.2. Before that, null had to be paired with another type.";
code = "<?php

// Accept null as input, when used as third argument of file_get_contents
function foo($b) {
    $s = file_get_contents(URL, false, $b);

    // Returns a string
    return shell_exec($s);
}

?>"
clearphp = "";
phpVersion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.2";
precision = "P_HIGH"

features[] = "null"
features[] = "typehint"
features[] = "nullable"

modifications[] = "Add `null` typehint to the code (PHP 8.0+)."
modifications[] = "Add `?` typehint to the code."

phpErrors[] = ""
modifications[] = ""

seeAlso[] =""