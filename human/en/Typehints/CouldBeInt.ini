name = "Type Could Be Integer";
description = "This rule marks arguments, class constants, properties and return types that can be set to ``int``.";
code = "<?php

// Accept an int as input 
function foo($b) {
    // Returns an int
    return $b + 8;
}

?>"
clearphp = "";
phpVersion = "";
severity = "S_MAJOR";
timetofix = "T_QUICK";
exakatSince = "2.1.4";
precision = "P_HIGH"
modifications[] = "Add `int` typehint to the code."

phpErrors[] = "";

seeAlso[] = ""

features[] = "integer"
features[] = "type"

