name = "Could Be Generator";
description = "Return value may be typed ``generator``.

<?php

// Yield makes foo() a generator
function foo() {
    yield 1; 
    // Returns an int
    return $b + 8;
}

?>
";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.2.2";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
precision = "P_HIGH";
modifications[] = "Add `\Generator` typehint to the method."

precision = 'P_VERY_HIGH'

features[] = 'class'

seeAlso[] = "`class <https://www.php.net/manual/en/language.oop5.basic.php#language.oop5.basic.class>`_"

phpErrors[] = ""

phpVersion = "";
