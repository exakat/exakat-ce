name = "idn_to_ascii() New Default";
description = "The default parameter value of idn_to_ascii() and idn_to_utf8() is now `INTL_IDNA_VARIANT_UTS46` instead of the deprecated ``INTL_IDNA_VARIANT_2003``.";
code = "<?php

echo idn_to_ascii('täst.de'); 

?>";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.2.3";
precision = "P_VERY_HIGH";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
modifications[] = "Explicitly add the second parameter to the idn_to_ascii() and idn_to_utf8() functions.";
; Alternative to make this code go away. 
; One by possible solution

features[] = "internationalization";
; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features

seeAlso[] = "`idn_to_ascii <https://www.php.net/manual/en/function.idn-to-ascii.php>`_";
seeAlso[] = "`idn_to_utf8 <https://www.php.net/manual/en/function.idn-to-utf8.php>`_";
seeAlso[] = "`Unicode IDNA Compatibility Processing <http://unicode.org/reports/tr46/>`_";
; List of links of interest

related[] = "";
; Possible other rules that might be related to this one. 
; Use "Folder/Name" format, one each
phpVersion = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.

phpDeprecation = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
phpErrors[] = "";
; A PHP error that may be emitted by the target faulty code

keywords[] = "default value";
; Human readable keywords for manual glossary
