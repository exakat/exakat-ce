name = "Do Not Cast To Int";
description = "Do not cast floats values to int. Uses conversion functions like intval(), round(), floor() or ceil() to convert the value to integer, with known behavior. 

Use functions like floor(), round() or ceil() : they use an explicit method for rounding, that helps keeping the side effects under control.";
code = "<?php

    // echoes 7!
    echo (int) ( (0.1 + 0.7) * 10 ); 

?>";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "0.10.6";
precision = "P_HIGH";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
modifications[] = "Upgrade PHP version to 8.0, as those behavior are now the same";
modifications[] = "Use one of the dedicated function : intval(), round(), floor() or ceil()";
; Alternative to make this code go away. 
; One by possible solution

features[] = "cast";
features[] = "integer";
features[] = "float";
; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features

seeAlso[] = "`Integers <https://www.php.net/manual/en/language.types.integer.php>`_";
; List of links of interest

related[] = "";
; Possible other rules that might be related to this one. 
; Use "Folder/Name" format, one each
phpVersion = "8.0-";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.

phpDeprecation = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
phpErrors[] = "";
; A PHP error that may be emitted by the target faulty code

keywords[] = "";
; Human readable keywords for manual glossary

changedBehavior = "";
; Which version did that behavior changed
changedBehavior_details = "";

