name = "Php 7.4 New Classes";
description = "New classes, introduced in PHP 7.4. If classes where created with the same name, in current code, they have to be moved in a namespace, or removed from code to migrate safely to PHP 7.4.

The new classes are : 

+ ``ReflectionReference``
+ ``WeakReference``

<?php

namespace {
    // Global namespace
    class WeakReference {
        // Move to a namespace
        // or, remove this class
    }
}

namespace B {
    class WeakReference {
        // This is OK : in a namespace
    }
}

?>

";
clearphp = "";
phpVersion = "7.4-";
severity = "S_MAJOR";
timetofix = "T_SLOW";
exakatSince = "1.0.4";

precision = "P_VERY_HIGH"

modifications[] = "Move the current classes with the same names into a distinct domain name";
modifications[] = "Change the name of the class";

features[] = "class"

seeAlso[] = "`New Classes and Interfaces <https://www.php.net/manual/en/migration74.classes.php>`_"

phpErrors[] = ""

