name = "Trigger Errors";
description = "List of situations where user errors are triggered.

PHP errors are triggered with trigger_error().

<?php
if ($divisor == 0) {
    trigger_error('Cannot divide by zero', E_USER_ERROR);
}
?>

";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "0.8.4";
precision = 'P_VERY_HIGH'

modifications[] = ''

features[] = 'error'

seeAlso[] = "`trigger_error <https://www.php.net/trigger_error>`_"

phpErrors[] = ""

phpVersion = "";
