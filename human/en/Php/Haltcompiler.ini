name = "__halt_compiler";
description = "__halt_compiler() usage.

<?php

// open this file
$fp = fopen(__FILE__, 'r');

// seek file pointer to data
fseek($fp, __COMPILER_HALT_OFFSET__);

// and output it
var_dump(stream_get_contents($fp));

// the end of the script execution
__halt_compiler(); the installation data (eg. tar, gz, PHP, etc.)

?>
";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "0.8.4";
precision = 'P_VERY_HIGH'

modifications[] = ""

features[] = 'halt-compiler'

seeAlso[] = "`__halt_compiler <https://www.php.net/manual/en/function.halt-compiler.php>`__"

phpErrors[] = ""

phpVersion = "";
