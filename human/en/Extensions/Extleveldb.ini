name = "ext/leveldb";
description = "PHP Binding for LevelDB.

LevelDB is a fast key-value storage library written at Google that provides an ordered mapping from string keys to string values.

<?php

$db = new LevelDB($leveldb_path);

$batch = new LevelDBWriteBatch();
$batch->set('batch_foo', 'batch_bar');
$batch->put('batch_foo2', 'batch_bar2');
$batch->delete('batch_foo');

$db->write($batch);

$batch->clear();
$batch->delete('batch_foo2');
$batch->set('batch_foo', 'batch again');

?>

";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "1.1.7";
precision = "P_VERY_HIGH"
seeAlso[] = "`ext/leveldb on Github <https://github.com/reeze/php-leveldb>`_"
seeAlso[] = "`Leveldb <https://github.com/google/leveldb>`_"

features[] = ""

phpErrors[] = ""

phpVersion = ""

modifications[] = ""
