name = "ext/memcached";
description = "Extension ext-memcached.

This extension uses the libmemcached library to provide an API for communicating with memcached servers. It also provides a session handler (`memcached`).

<?php
$m = new Memcached();
$m->addServer('localhost', 11211);

$m->set('foo', 100);
var_dump($m->get('foo'));
?>

";
clearphp = "";
severity = "";
timetofix = "";
exakatSince = "0.8.4";
precision = "P_VERY_HIGH"

features[] = ""

phpErrors[] = ""

phpVersion = ""

seeAlso[] = "`ext/memcached manual <https://www.php.net/manual/en/book.memcached.php>`_"
seeAlso[] = "`memcached <http://www.memcached.org/>`_"

