name = "ext/xattr";
description = "Extensions xattr.

The xattr extension allows for the manipulation of extended attributes on a filesystem.

<?php
$file = 'my_favourite_song.wav';
xattr_set($file, 'Artist', 'Someone');
xattr_set($file, 'My ranking', 'Good');
xattr_set($file, 'Listen count', '34');

/* ... other code ... */

printf('You\'ve played this song %d times', xattr_get($file, 'Listen count')); 
?>

";
clearphp = "";
severity = "S_NONE";
timetofix = "T_NONE";
exakatSince = "0.12.4";
precision = "P_VERY_HIGH"

features[] = "file"

phpErrors[] = ""

phpVersion = ""

seeAlso[] ="`xattr <https://www.php.net/manual/en/book.xattr.php>`_"
seeAlso[] = "`Extended attributres <https://en.wikipedia.org/wiki/Extended_file_attributes>`_"

modifications[] = ""