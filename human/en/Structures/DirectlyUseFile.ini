name = "Directly Use File";
description = "Some PHP functions have a close cousin that work directly on files. This is faster and less code to write.

* md5() => md5_file()
* highlight_string() => highlight_file(), show_source()
* parsekit_compile_string() => parsekit_compile_file()
* parse_ini_string() => parse_ini_file()
* sha1() => sha1_file()
* simplexml_load_string() => simplexml_load_file()
* yaml_parse() => yaml_parse_file()
* hash() => hash_file()
* hash_hmac() => hash_mac_file()
* hash_update() => hash_update_file()
* recode() => recode_file()
* recode_string() => recode_file()";
code = "<?php

// Good way
$file_hash = hash_file('sha512', 'example.txt');

// Slow way
$file_hash = hash('sha512', file_get_contents('example.txt'));

?>";
clearphp = "";
severity = "S_MINOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_INSTANT";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "1.5.5";
precision = "P_VERY_HIGH";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
modifications[] = "`hash_file <https://www.php.net/manual/en/function.hash-file.php>`_";
; Alternative to make this code go away. 
; One by possible solution

features[] = "";
; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features

seeAlso[] = "";
; List of links of interest

related[] = "";
; Possible other rules that might be related to this one. 
; Use "Folder/Name" format, one each
phpVersion = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.

phpDeprecation = "";
; Versions when this feature was deprecated. Beyond is removed, and before is fine.
; This might need a range : 7.3-7.4.
phpErrors[] = "";
; A PHP error that may be emitted by the target faulty code

keywords[] = "";
; Human readable keywords for manual glossary

changedBehavior = "";
; Which version did that behavior changed
changedBehavior_details = "";

