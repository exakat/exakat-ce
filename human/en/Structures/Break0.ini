name = "Break With 0";
description = "It is not possible to break 0 : it makes no sense. Break 1 is the minimum, and is the default value.

<?php
    // Can't break 0. Must be 1 or more, depending on the level of nesting.
    for($i = 0; $i < 10; $i++) {
        break 0;
    }

    for($i = 0; $i < 10; $i++) {
        for($j = 0; $j < 10; $j++) {
            break 2;
        }
    }

?>

";
clearphp = "";
phpVersion = "5.4-";
severity = "S_MINOR";
timetofix = "T_QUICK";
exakatSince = "0.8.4";
precision = "P_HIGH"

precision = "P_VERY_HIGH"

seeAlso[] = ""

; Alternative to make this code go away. 
; One by possible solution
modifications[] = "Remove 0, or the break"
; A PHP error that may be emitted by the target faulty code
phpErrors[] = "'break' operator accepts only positive integers"

features[] = ""

