name = "Wrong Locale";
description = "Checks the locale used in the code against a library. 

<?php

// what language ? 
setLocale(LC_ALL, 'hx');

// utf8 actually needs a - : utf-8
setLocale(LC_ALL, 'utf8');

?>

";
clearphp = "";
severity = "S_MAJOR";
; from : S_NONE, S_MINOR, S_MAJOR, S_CRITICAL
timetofix = "T_QUICK";
; from : T_INSTANT, T_QUICK, T_SLOW
exakatSince = "2.4.2";
; from : P_VERY_HIGH, P_HIGH, P_MEDIUM, P_LOW, P_UNKNOWN
precision = "P_HIGH";
;Versions of PHP where this rule is applicable. 
;8.0+, 7.4-...
phpVersion = ""

; List of links of interest
seeAlso[] = "";

; Name of a PHP feature. 
; This should match one of the available feature description in humane/en/Features
features[] = "locale";

; Alternative to make this code go away. 
; One by possible solution
modifications[] = "Use a valid locale"

; A PHP error that may be emitted by the target faulty code
phpErrors[] = ""

; Optional parameters
; Duplicate and increment numbers to add more : parameter1, paramter2, etc. 
[parameter1]
name="otherLocales";
default="";
type="array";
description="Other accepted locales, comma separated";

[parameter2]
name="maxPositions";
default="3";
type="integer";
description="Number of argument in setLocale() to be tried.";
