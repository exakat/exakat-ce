name = "Unkown Regex Options";
description = "Regex support in PHP accepts the following list of options : ``eimsuxADJSUX``. 

All other letter used as option are not supported : depending on the situation, they may be ignored or raise an error.

<?php

// all options are available
if (preg_match('/\d+/isA', $string, $results)) { }

// p and h are not regex options, p is double
if (preg_match('/\d+/php', $string, $results)) { }

?>

";
clearphp = "";
severity = "S_MAJOR";
timetofix = "T_SLOW";
exakatSince = "0.8.4";
precision = 'P_HIGH'

modifications[] = "Remove the unknown options"
modifications[] = "Replace the option with a valid one"
modifications[] = "Fix any syntax typo in the regex"

features[] = 'regex'

seeAlso[] = "`Pattern Modifiers <https://www.php.net/manual/en/reference.pcre.pattern.modifiers.php>`_"

phpErrors[] = ""

phpVersion = "";

